#:schema https://github.com/sin-ack/agent-run/raw/master/schema.json

[global]
mount = ["..", "~/.cache/bazel"]

[tools.pi]
mount = [
    "~/.pi",
    # Pi writes bash outputs to /tmp/pi-bash-*.log.
    "/tmp",
]
